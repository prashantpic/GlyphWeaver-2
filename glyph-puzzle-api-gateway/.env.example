# Server Configuration
PORT=3000
LOG_LEVEL=info

# Redis Configuration (for rate limiting, circuit breaker, etc.)
REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=
# REDIS_DB=0

# JWT Authentication Configuration
JWT_SECRET_OR_PUBLIC_KEY=your-super-secret-jwt-key-or-public-key-pem-string
JWT_ISSUER=glyph-puzzle-api
JWT_AUDIENCE=glyph-puzzle-clients
# JWT_ALGORITHM=HS256 (default if secret is used, or RS256/ES256 if public key is used)
# JWT_EXPIRES_IN=1h

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
CORS_ALLOWED_ORIGINS=* # Or specify domains: http://localhost:3001,https://app.glyphpuzzle.com

# Service Discovery Configuration (Conceptual - if using Consul, Kubernetes, etc.)
SERVICE_DISCOVERY_PROVIDER_URL=http://localhost:8500 # Example: Consul agent URL
# SERVICE_DISCOVERY_TOKEN=

# Canary Release Configuration (Conceptual)
# CANARY_BACKEND_SERVICE_URL=http://localhost:4001 # If a separate canary instance exists
# CANARY_TRAFFIC_PERCENTAGE=5 # Percentage (0-100) of traffic to canary

# Circuit Breaker Configuration (Can be overridden per proxy policy)
# CIRCUIT_BREAKER_ERROR_THRESHOLD_PERCENTAGE=50 # Default error threshold
# CIRCUIT_BREAKER_RESET_TIMEOUT_MS=30000 # Default reset timeout in ms

# Backend Service URL (Fallback if service discovery is not used or fails)
# This is typically configured in gateway.config.yml or discovered
# FALLBACK_BACKEND_SERVICE_URL=http://localhost:4000